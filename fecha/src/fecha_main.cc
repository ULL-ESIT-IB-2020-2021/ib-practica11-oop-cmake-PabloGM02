#include <iostream>
#include "fecha.h"

int main(int argc, char* argv[]){
    if(argc != 3){
        std::cout << "Introduzca una fecha, un número, y un nombre de fichero de texto." << std::endl;
        std::cout << "Este programa contiene una clase Fecha que le permitirá trabajar de diferentes modos." << std::endl;
        return 0;
    }


}